const arr = [1,2,3,4,5,6,7,8]

const arrayLength = arr.length //length of the arr
console.log(arrayLength)

const shiftMethods = arr.shift() // first element from the array is removed
console.log(arr);

const unshiftMethods = arr.unshift(1) // elemnet is added at 0 index
console.log(arr);

const arrayPop = arr.pop() //last element of the array is removed
console.log(arr)

const arrayPush = arr.push(8) //element is added to last index of the array
console.log(arr);
